#include <bits/stdc++.h>
using namespace std;

int v[255 * 255];
int m[255][255];

int main() {

	int n, m;
	cin >> n >> m;

	for (int i = 0; i < n * m; ++i) {
		scanf("%d", v + i);
	}

	sort(v, v + (n * m));
	reverse(v, v + (n * m));

	m[0][0] = [0];

	int k = min()
	for (int i = 1; i < n; ++i) {
		
	}

	return 0;
}